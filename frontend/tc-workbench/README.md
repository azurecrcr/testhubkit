# TC Workbench Frontend

Bundle build wraps legacy IIFE sources via `scripts/build_tc_workbench.sh`.
Source features live under `static/js/tc_workbench/features/`.

## tc-gen-chat
React bundle source is not in repo; dist at `static/dist/tc-gen-chat/`.
Bridge API: `window.TcGenChatXUi`.
